
  # AI Property Report UX_UI (1)

  This is a code bundle for AI Property Report UX_UI (1). The original project is available at https://www.figma.com/design/EmaRX07hEJK2XSazCp87qA/AI-Property-Report-UX_UI--1-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  